<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practica3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica3">
        <activity android:name=".VistaventasActivity"></activity>
        <activity android:name=".EditarProducto" />
        <activity android:name=".EditarClienteActivity" />
        <activity android:name=".VentaActivity" />
        <activity android:name=".ProductoActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".BienvenidaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>